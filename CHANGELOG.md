# 更新记录（CHANGELOG）

本文件记录鲲鹏对外版本的高层里程碑。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

> 本文档由内部开发日志摘要整理，只保留面向用户与贡献者的高层变化，不包含内部过程细节。

## [Unreleased] - 2026-09（个性化 fork）

- **修复斜杠命令双层路由冲突**：仓库里存在两套命令系统——legacy 注册表（`/auto /clear /compact /cwd /skill /mcp /evolve /cost`，位于 `src/lib/agent/commands/`）在 sendMessage 中先执行且**吞掉一切 `/` 开头的输入**（未注册命令一律回执"未知命令"，路径开头的普通消息也被吃掉），导致 DSH 会话命令层（`/compact /plan /goal /resume /help`）整层不可达（用户输入 `/resume` 得到的是 legacy 的"未知命令"回执）。现合并为统一路由：DSH 命令优先 → legacy 命令（`/compact` 在非 Harness 引擎聊天回落传统上下文压缩）→ 常见他牌命令词给未知指引 → 其余 `/` 开头输入（含路径）按普通消息放行。`/help` 输出合并两套命令清单；legacy 命令回执也补上了用户回显与持久化。
- **修复 L3 映射随应用重启丢失**：`chatSessionId → DSH sessionId` 注册表原本只在内存里——应用重启一次，所有聊天会话的 Harness 会话映射即被抹掉，跨轮复用随之中断（用户「继续」回到纯文本回放，上下文体感全丢）。注册表现在经 localStorage（键 `kunpeng-harness-sessions`）持久化，重启后自动还原；DSH 会话文件本就在磁盘上永生，映射不丢即续接不丢。
- **新增 `/resume [序号]`**：应用重启丢失映射后的手动恢复手段。不带参数列出磁盘上最近 12 个持久化 Harness 会话（id/时间/大小/workspace）；`/resume <序号|sessionId前缀>` 把当前聊天会话重新绑定到该记录，下一轮即恢复完整工作记忆。绑定因子（模型/技能签名）与轮次启动时的计算一致。
- **修复长任务被误杀**：`session/prompt` 原为 60 分钟墙钟硬超时——一轮合法的数小时级 agentic 任务（串联多个 30 分钟付费工具调用）会在第 60 分钟被强制中止。改为**静默超时**：轮次期间任何 ACP 通道活动（模型输出分片、工具事件、host 日志）都会为计时器续期，只有连续 45 分钟完全无声才判死（窗口 > 单工具 30 分钟上限，为静默长思考留缓冲）。

- **聊天斜杠命令**：`/compact`（手动压缩 Harness 会话历史——起临时进程 resume 目标会话、经 sidecar 通道调官方 compactNow，不消耗对话轮次）、`/plan [off]`（计划模式：先设计呈交批准再执行）、`/goal <目标>|clear`（会话长期目标，每轮注入）、`/help`。命令由前端直接执行，不进入模型对话。
  - 命令反馈：命令回显即时入列；`/compact` 执行期间显示分阶段进度（恢复会话→生成摘要）并锁定输入框，完成后原地刷新为结果回执（含压缩条数与 token 数）；修复 sidecar 响应信封解析错误导致的"成功却提示无可压缩历史"。
  - 输入其他工具的常见命令（`/clear` `/resume` 等）会得到"未知命令 + 本产品命令清单 + 等价操作指引"的回执，不再误发给模型。
- **Harness 跨轮会话复用（L3 根治）**：同一聊天会话连续轮次通过 ACP `session/resume` 恢复上一轮的持久化 DSH 会话——历史（工具调用、中间产物、推理）完整保留，不再依赖 ≤160K 文本回放；模型/技能变化或恢复失败自动降级旧行为；失败轮的 sessionId 同样保留，说「继续」即可从磁盘恢复失败轮的完整上下文。
- **Harness 失败断点保全**：DeepSeek API 中断（如 503 重试耗尽）不再让整轮对话从历史里蒸发——失败轮的用户请求、错误原因与已执行步骤（含付费勿重做清单）会写入对话历史，说「继续」即可接上断点。
- **续接降级**：Harness 轮内故障且未发生任何付费调用时，自动降级到 DeepSeek 普通模式并携带已完成步骤上下文；一旦有付费调用启动过，仍拒绝重放（防重复扣费）。
- **新 Agent「制片人」**：梗概→EP 扩充（高潮点/基调）→生活化台词剧本（含语气词规范）→长短镜头拆镜（自然过渡、【人为辅助】标注传递）→批量生图→生视频→交付拼接前检查单的专家角色。
- **新技能**：`showrunner`（制片人 SOP）、`seedance-prompt`（Seedance 2.0 提示词工厂，vendored from MapleShaw/seedance2.0-prompt-skill, MIT）、`cinematic-director`（电影导演方法库 + 20 导演风格，vendored from wuwangzhang1216/DirectorSKILL, MIT）。

## [2.8.6] - 2026-07

当前版本。近期工作重点：

- **稳定性加固**：画布功能全面审计修复（参考传参、路由、状态、agent 工具、持久化等 20 项）；画布任务轮询/恢复机制修复（11 项竞态与状态机漏洞）。
- **配额防线**：修复 localStorage 配额超限导致的打开项目失败问题，增加三层防线（超大缓存不进本地存储、空画布覆写保护、数据恢复）。
- **动效与导演思维**：动效 agent 默认注入导演思维；概念/媒介演示库扩充至 30 个场景；AE 动效引擎新增 8 个生命感运动原语、6 个新浪潮 style kit、15 个签名场景。

## [2.7.x] - 2026-07

- **KPMotion 运动运行时**（2.7.0）：AI 配特效、自由页面、口播包装全线重构。
- **AI 设计准则重写**（2.7.1）：去模板化、风格问询、字体收敛；修复导出超时浮点卡死问题。
- **设计维度扩容**（2.7.2）：运动原语 7 → 12，新增质感/排版装置体系与 9 种构图语法。
- **艺术方向套件（Style Kits）**（2.7.3 / 2.7.4）：9 种美学人格一键激活，随后扩充至 10 家族 128 种（全手写）。

## [2.6.x] - 2026-07

- **AI 剪辑重构**（2.6.0）：口播审片系统（speechAudit），从「文本相似度」判定升级为「信息增量 + 音频证据链」审片。
- **剪辑体验改进**（2.6.1）：左侧抽屉化、文稿带标点、字级拖选删除，默认自动检测。
- **存储可靠性**（2.6.2）：quota 主动防线，超大缓存不再写入 localStorage。

## [2.5.x] - 2026-06/07

- **风格库（Style Library）**（2.5.7）：风格可视化选择 + AI 静默改写；后续迭代预生成 DNA、画布音频节点、工坊视频并行、画布图回传工坊关联等。
- **ASR 链路升级**：剪辑转写切换到豆包录音文件识别（AUC 大模型），Whisper 降级为备用；修复长音频超 25MB 上限的 413 错误；画布 agent 新增 `canvas_transcribe` 工具。
- **工程可移植性**（2.5.7-i）：工程保存/导入（zip 打包工坊 + 画布 + 资产，跨机器可移植）。
- **画布批量生成**（2.5.9-a）：左栏上传支持多选，批量生成多节点。
- 以及多项修复：mini 全能参考 role 映射、丽帧 return_last_frame 与参考图冲突、COS 上传等。

## [2.0 - 2.4] - 2026-06

- **五大模块成型**：对话编程助手、可视化节点画布、工坊（分镜/脚本驱动的批量生产）、AI 剪辑（P0–P8 全量改造）、文案工作室（AI 写作工具）。
- **页面引擎视觉升级**：高端网页 / 高级 PPT 级别输出；AI 特效系统接入 250+ 页面模板。
- **多渠道生成引擎接入**：图像（多引擎容灾降级链）、视频（Seedance 系、丽帧等）、语音（Doubao Seed-Audio 台词配音）。
- **画布能力完善**：右键上传媒体、节点替换、@ 提及高亮、asset:// 协议解析、即梦结果写回画布等。
- **外部集成**：微信 + 飞书消息集成与多媒体能力。
- **Windows 平台适配**：建立 macOS / Windows 双平台迁移基线。

---

更早的历史变化未逐一整理，如有需要请查阅提交历史。
